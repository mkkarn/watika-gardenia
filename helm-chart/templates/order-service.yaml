apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.orderService.name }}
  labels:
    app: {{ .Values.orderService.name }}
spec:
  replicas: 3
  selector:
    matchLabels:
      app: {{ .Values.orderService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.orderService.name }}
    spec:
      containers:
        - name: {{ .Values.orderService.name }}
          image: "{{ .Values.images.orderService.repository }}:{{ .Values.images.orderService.tag }}"
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: {{ .Values.orderService.resources.requests.cpu }}
              memory: {{ .Values.orderService.resources.requests.memory }}
            limits:
              cpu: {{ .Values.orderService.resources.limits.cpu }}
              memory: {{ .Values.orderService.resources.limits.memory }}
          env:
            - name: ORDERS_DB_HOST
              value: {{ .Values.ordersDatabase.connectionString | quote }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.orderService.name }}
  labels:
    app: {{ .Values.orderService.name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.orderService.name }}
  ports:
    - port: 80
      targetPort: 8080

